# Proxy Integration Notes
- Generate proxies from the WSDL/interface definition — don't hand-write structures that should be generated.
- Confirm synchronous vs. asynchronous pattern matches the business process's actual tolerance for latency.
