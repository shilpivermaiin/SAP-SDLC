# EDI Integration Notes
- Confirm EDI standard (EDIFACT, X12, etc.) and version with the trading partner before mapping.
- Map to IDoc as the SAP-side intermediary where the standard SAP EDI subsystem is in use.
- Test with real partner-provided sample files, not just internally generated test data.
